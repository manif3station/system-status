# DD-072

## Title

Package `system-status.disk`, `system-status.load`, and `system-status.temperature`.

## Method

TDD

## Acceptance

- `dashboard system-status.disk` is packaged from the reference script and returns the existing JSON contract
- `dashboard system-status.load` is packaged from the reference script and returns the existing JSON contract
- `dashboard system-status.temperature` is packaged from the reference script and returns the existing JSON contract
- the skill includes README, docs, `.env`, `Changes`, `tickets`, and `t/`
- Linux, macOS, and Windows code paths are covered by automated tests
- Docker tests and 100% coverage pass
- docs, commit, and push are completed

## Status

Done
