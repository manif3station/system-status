# EPIC-046

## Title

Add an explicit open-source license to the `system-status` skill.

## Outcome

Make the `system-status` skill ship with a clear MIT license so users and maintainers can reuse and distribute it without ambiguity.

## Tickets

- `DD-073` Add an explicit MIT license to the `system-status` skill
