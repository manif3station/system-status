# SOW

## SOW-001

Create a proper `system-status` DD skill from the three reference scripts already present in the skill repository, with an explicit MIT license for reuse and distribution.

## Scope

- package `dashboard system-status.disk`
- package `dashboard system-status.load`
- package `dashboard system-status.temperature`
- preserve the JSON contracts from the reference scripts
- standardize the skill with docs, tests, coverage, commit, and push gates
- cover Linux, macOS, and Windows code paths
- add an explicit MIT license and README license documentation
